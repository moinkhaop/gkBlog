{
  "name": "gkblog",
  "private": true,
  "scripts": {
    "build": "prisma generate && echo '--- Searching for ContentType in generated client ---' && (grep 'ContentType' ./node_modules/.prisma/client/index.d.ts || echo '--- ContentType NOT FOUND in generated file ---') && next build",
    "dev": "turbo run dev --parallel",
    "lint": "turbo run lint",
    "clean": "turbo run clean",
    "format": "prettier --write .",
    "start": "turbo run start"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "prettier": "^3.5.3",
    "prettier-plugin-tailwindcss": "^0.6.11",
    "turbo": "^2.5.2"
  },
  "packageManager": "pnpm@10.10.0"
}
